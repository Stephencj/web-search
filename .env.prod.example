# Production Environment Configuration
# Copy this file to .env on your production server

# Domain Configuration
DOMAIN=refinedaf.com
ACME_EMAIL=your-email@example.com

# Meilisearch Master Key (generate a secure random key)
# Generate with: openssl rand -base64 32
MEILI_MASTER_KEY=your-secure-master-key-here

# YouTube OAuth2 (optional - for premium playback features)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create/select a project
# 2. Enable YouTube Data API v3
# 3. Configure OAuth consent screen
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add redirect URI: https://refinedaf.com/api/accounts/callback/youtube
WEBSEARCH_OAUTH__YOUTUBE_CLIENT_ID=
WEBSEARCH_OAUTH__YOUTUBE_CLIENT_SECRET=

# Encryption key for OAuth tokens (required if using OAuth)
# Generate with: python -c "import secrets; import base64; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
WEBSEARCH_OAUTH__ENCRYPTION_KEY=
